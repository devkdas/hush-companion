This directory is reserved for future local voice assets.

The current Kokoro server uses the configured Kokoro voice IDs from `HUSH_KOKORO_*_VOICE` environment variables and does not read reference recordings from this directory. Do not add private recordings or generated audio to source control.
